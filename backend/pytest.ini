[pytest]
testpaths = tests
asyncio_mode = auto
addopts = -v --tb=short
markers =
    e2e: marks tests as end-to-end (requires real Supabase + Gemini, gated by RUN_E2E=1)
filterwarnings =
    ignore::DeprecationWarning
