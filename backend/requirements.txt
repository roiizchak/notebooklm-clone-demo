# Direct dependencies pinned to exact versions (F6b) for reproducible Vercel
# builds. Bump deliberately + re-run the test suite. Transitive deps are left to
# the resolver (no full lockfile/hashes to keep this readable on Windows).

fastapi==0.136.1
uvicorn[standard]==0.46.0
python-dotenv==1.2.2
pydantic==2.13.4
pydantic-settings==2.14.1

# Supabase
supabase==2.30.0

# Auth (JWT JWKS verification)
python-jose[cryptography]==3.5.0

# Gemini SDK
google-genai==2.0.1

# HTTP client (URL ingestion + JWKS fetch)
httpx==0.28.1

# Source ingestion
pypdf==6.11.0
python-docx==1.2.0
trafilatura==2.0.0
youtube-transcript-api==1.2.4
